# Allow installs to proceed when a library's peer-dependency range
# does not yet include the newer major versions we use (e.g. React 19).
# Yarn classic ignores these as warnings; npm errors out unless this is set.
# This is required for Render Static Site builds (which use npm by default).
legacy-peer-deps=true
